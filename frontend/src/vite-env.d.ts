/// <reference types="vite/client" />

// Injected by vite.config.ts via `define`. Local-time build timestamp in
// "YYYY-MM-DD HH:MM" format, shown in Layout footer + matches the mobile
// app's settings → אודות → גרסה line.
declare const __BUILD_TIMESTAMP__: string
